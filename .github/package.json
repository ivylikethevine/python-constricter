{
  "name": "ci-markdown-tools",
  "private": true,
  "description": "Pins CI's Markdown tools and their dependency tree, for `npm ci --prefix .github`.",
  "devDependencies": {
    "markdownlint-cli2": "0.23.3",
    "prettier": "3.9.8"
  }
}
